name: drafted-web
region: ams
static_sites:
  - name: website
    git:
      repo_clone_url: https://github.com/kylietlu/drafted-web.git
      branch: main
    source_dir: /
    environment_slug: hugo
    build_command: hugo --gc --minify
    output_dir: public
    index_document: index.html
